# =====================================================================
# CORE — required on every machine (laptop + VM)
# =====================================================================
pandas>=2.2
numpy>=1.26
requests>=2.31
yfinance>=0.2.40
python-dotenv

# Web terminal (FastAPI)
fastapi
uvicorn

# Scheduler
apscheduler
pytz

# ML (meta-model + ml_train)
scikit-learn
lightgbm
joblib

# =====================================================================
# Optional extras — see requirements-optional.txt
# (streamlit, plotly, transformers, torch, feedparser,
#  matplotlib, gspread)
# =====================================================================